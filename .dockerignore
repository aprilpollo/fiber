# Keep secrets out of the build context — COPY . . would otherwise pull .env
# into the builder stage and its cache.
.env
.env.*
!.env.example

# Version control and editor/OS noise
.git
.gitignore
.github
.DS_Store
.vscode
.idea

# Docs and Docker files: not needed to compile the binary
README.md
docker/
